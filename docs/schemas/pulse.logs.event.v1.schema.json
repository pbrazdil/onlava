{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pulse.dev/schemas/pulse.logs.event.v1.schema.json",
  "title": "pulse.logs.event.v1",
  "description": "Schema for `pulse logs --jsonl` and `pulse logs --json` output lines.",
  "type": "object",
  "required": ["schema_version", "app", "id", "pid", "stream", "output", "created_at"],
  "properties": {
    "schema_version": { "const": "pulse.logs.event.v1" },
    "app": {
      "type": "object",
      "required": ["name", "root"],
      "properties": {
        "name": { "type": "string" },
        "root": { "type": "string" }
      },
      "additionalProperties": false
    },
    "id": { "type": "integer", "minimum": 1 },
    "pid": { "type": "string" },
    "stream": { "type": "string", "enum": ["stdout", "stderr"] },
    "output": { "type": "string" },
    "created_at": { "type": "string" }
  },
  "additionalProperties": false
}
